START TRANSACTION;

CREATE TABLE "Departments" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Description" text NULL,
    "Name" character varying(100) NOT NULL,
    "Number" character varying(32) NULL,
    "StoreId" integer NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "Key" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Departments" PRIMARY KEY ("Id"),
    CONSTRAINT "FK_Departments_Stores_StoreId" FOREIGN KEY ("StoreId") REFERENCES "Stores" ("Id") ON DELETE CASCADE
);

CREATE INDEX "IX_Departments_Deleted" ON "Departments" ("Deleted");

CREATE UNIQUE INDEX "IX_Departments_Key" ON "Departments" ("Key");

CREATE INDEX "IX_Departments_Name" ON "Departments" ("Name");

CREATE INDEX "IX_Departments_Number" ON "Departments" ("Number");

CREATE INDEX "IX_Departments_StoreId" ON "Departments" ("StoreId");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220317151235_CreateDepartmentTable', '6.0.3');

COMMIT;
