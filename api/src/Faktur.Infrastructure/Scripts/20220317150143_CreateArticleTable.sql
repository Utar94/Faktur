START TRANSACTION;

CREATE TABLE "Articles" (
    "Id" integer GENERATED BY DEFAULT AS IDENTITY,
    "Description" text NULL,
    "Gtin" bigint NULL,
    "Name" character varying(100) NOT NULL,
    "CreatedAt" timestamp with time zone NOT NULL DEFAULT (now()),
    "CreatedById" uuid NOT NULL,
    "Deleted" boolean NOT NULL DEFAULT FALSE,
    "DeletedAt" timestamp with time zone NULL,
    "DeletedById" uuid NULL,
    "Key" uuid NOT NULL DEFAULT (uuid_generate_v4()),
    "UpdatedAt" timestamp with time zone NULL,
    "UpdatedById" uuid NULL,
    "Version" integer NOT NULL DEFAULT 0,
    CONSTRAINT "PK_Articles" PRIMARY KEY ("Id")
);

CREATE INDEX "IX_Articles_Deleted" ON "Articles" ("Deleted");

CREATE INDEX "IX_Articles_Gtin" ON "Articles" ("Gtin");

CREATE UNIQUE INDEX "IX_Articles_Key" ON "Articles" ("Key");

CREATE INDEX "IX_Articles_Name" ON "Articles" ("Name");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20220317150143_CreateArticleTable', '6.0.3');

COMMIT;
